const leaderCard = ({
  leadershipData,
}: {
  leadershipData: {
    title: string;
    location: string;
    description: string;
    date: string;
  };
}) => {
  return <div>leaderCard</div>;
};

export default leaderCard;
